/*let evenNumbers = N;  

    while (evenNumbers > 0) {
        evenNumbers = evenNumbers + 2;
    }

    console.log(evenNumbers);

    let n = parseInt(gets());*/


let n = parseInt(gets());
let par = 2;

    while(par <= n) {
        console.log(par);
        par = par + 2;
    }

    
